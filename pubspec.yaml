name: cat_project
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.14.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono: ^1.17.0

flutter:
  uses-material-design: true
